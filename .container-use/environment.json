{
  "workdir": "/workdir",
  "base_image": "ubuntu:24.04",
  "setup_commands": [
    "apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y git curl ca-certificates docker.io",
    "curl -fsSL https://dl.dagger.io/dagger/install.sh | DAGGER_VERSION=v0.21.8 BIN_DIR=/usr/local/bin sh"
  ]
}
