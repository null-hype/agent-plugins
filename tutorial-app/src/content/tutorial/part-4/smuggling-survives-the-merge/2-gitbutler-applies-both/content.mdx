---
type: lesson
title: An illustrative clean merge
template: acp-trace
terminal: false
editor: true
previews:
  - - 4173
    - Client
  - - 4174
    - Agent
prepareCommands:
  - npm install
mainCommand: npm run dev
custom:
  acpTrace:
    traceFile: /acp-trace.json
    scenario: smuggling-v1
---

import AcpTraceBridge from '../../../../../components/AcpTraceBridge';

<AcpTraceBridge client:load />

# An illustrative clean merge

**Concept storyboard — not experimental evidence.** Solve replays authored turns;
it does not execute Git, Dagger, or agent reviews.

Solve reveals a clean textual merge illustration. The fixture reports a scratch
merge of `defenses.md`; it does not merge the described relay and Gunicorn source
changes, and it does not capture GitButler execution. Its abbreviated identifiers
are not reproducible source-tree evidence.

The warning is also authored. The proposed whitespace/image interaction is
unsupported: the real relay reads Content-Length and forwards bytes, while a
Python image bump does not update Gunicorn installed from the local codebase.
Request smuggling depends on disagreement about message boundaries; agreement or
rejection does not establish a bypass.

“Reveal storyboard diagnostic” displays existing fixture data. It runs no gate.
