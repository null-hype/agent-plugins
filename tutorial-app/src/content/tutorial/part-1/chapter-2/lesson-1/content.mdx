---
type: lesson
title: Authorization by Benutzer-ID
template: otel-warm-log
focus: /exercise.de
prepareCommands:
  - npm install
mainCommand: npm run dev
previews:
  - [4173, "Warm Log"]
terminal: false
editor:
  fileTree: true
custom:
  ruleTrace:
    commandFile: /exercise.de
    grammarFile: /AuthorizationGrammar.pkl
    runtimeFile: /authorization-grammar.json
    storyFile: /trace-story.json
    scenario: opentrader-idor
---

import RuleTraceBridge from '../../../../../components/RuleTraceBridge';

<RuleTraceBridge client:load />

Complete the sentence in `exercise.de`:

`Die API prüft die Berechtigung anhand _____ Benutzer-ID`

Enter **der**, the article required by this lesson's rule. Hover the diagnostic
to inspect the rule before you repair it. Once the phrase passes, the preview
reveals a mocked OpenTrader trace.

Expand its anomaly span. The security question is whether authorization relies
on a user-controlled ID. That is a separate check from the sentence exercise:
repairing the article reveals the trace; it does not repair the authorization flaw.

| File | What it contributes |
| --- | --- |
| `AuthorizationGrammar.pkl` | The sentence rule used by this exercise |
| `authorization-grammar.json` | The artifact used by the browser validator |
| `OpenTraderSecurityRules.pkl` | The policy describing authorization grounded in user-controlled input |
| `trace-story.json` | The authored trace and anomaly shown in the preview |

**Evidence scope:** this is a fixture-based demonstration. The browser does not
execute a live API exploit or prove a production authorization boundary.

Continue to the next chapter to inspect a recorded CI export and then work with
capability requests, grants, and observations.
