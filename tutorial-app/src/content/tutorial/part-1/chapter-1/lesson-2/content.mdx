---
type: lesson
title: Fingerspitzengefühl
template: otel-warm-log
focus: /translation.en
prepareCommands:
  - npm install
mainCommand: npm run dev
previews:
  - [4173, "Warm Log"]
terminal: false
editor:
  fileTree: false
custom:
  loanwordArc:
    lessonId: fingerspitzengefuhl
    translationFile: /translation.en
    vocabularyFile: /PersonalVocabulary.pkl
    runtimeFile: /loanword-runtime.json
    storyFile: /warm-log-story.json
    scenario: fingerspitzengefuhl-preservation
---

import LoanwordArcBridge from '../../../../../components/LoanwordArcBridge';

<LoanwordArcBridge client:load />

How would you express **Fingerspitzengefühl** in English?

Enter an answer in `translation.en`, then inspect the diagnostic.
This exercise's policy requires preserving the source spelling and admitting it
in `PersonalVocabulary.pkl`. It is a chosen vocabulary policy, not a claim that
English cannot express the idea.

Use **Solve** to inspect the completed files. Change the spelling or vocabulary
entry and watch which check responds. A rule is easier to assess when you can
see both an accepted example and the input it rejects.
