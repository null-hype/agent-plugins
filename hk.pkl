amends "package://github.com/jdx/hk/releases/download/v1.56.1/hk@1.56.1#/Config.pkl"
import "package://github.com/jdx/hk/releases/download/v1.56.1/hk@1.56.1#/Builtins.pkl"
// Using a coding agent? See https://hk.jdx.dev/agents

local linters = new Mapping<String, Step> {
    // Add linters here. Examples:
    // ["prettier"] = Builtins.prettier
    // ["eslint"] = Builtins.eslint
    // ["ruff"] = Builtins.ruff

    // Or define custom steps:
    // ["custom"] {
    //     glob = "**/*.py"
    //     check = "mypy {{ files }}"
    // }
}

hooks {
    ["pre-commit"] {
        fix = true
        stash = "git"
        steps = linters
    }
    ["fix"] {
        fix = true
        steps = linters
    }
    ["check"] {
        steps = (linters) {
            ["restic-check"] {
                check {
                    command = "bash .dagger/internal/devenv-base/hk/restic-check.sh"
                    effect = "read"
                }
            }
        }
    }
    ["pre-push"] {
        steps {
            // Syncs pushed commits to the Linear "Development" release.
            // See .dagger/internal/devenv-base/hk/linear-release-sync.sh.
            ["linear-release"] {
                check = "bash .dagger/internal/devenv-base/hk/linear-release-sync.sh"
            }
            // Backs up ~/.claude to the shared restic repo (disaster
            // recovery only). See .dagger/internal/devenv-base/hk/claude-session-backup.sh.
            ["claude-session-backup"] {
                check = "bash .dagger/internal/devenv-base/hk/claude-session-backup.sh"
            }
        }
    }
}
